# 1核2G服务器承载能力评估报告

## 📊 服务器配置

- **CPU**: 1核心
- **内存**: 2GB
- **架构**: Docker容器化部署
- **服务数量**: 4个容器（MongoDB + Backend + Frontend + AI Wallet）

---

## 🔍 资源分配分析

### 内存使用估算（2GB总容量）

| 服务 | 基础内存 | 峰值内存 | 说明 |
|------|---------|---------|------|
| **MongoDB** | 300MB | 500MB | 数据库服务，根据数据量增长 |
| **Node.js Backend** | 200MB | 400MB | 主要业务逻辑，处理API请求 |
| **Nginx Frontend** | 50MB | 100MB | 静态文件服务 + 反向代理 |
| **AI Wallet** | 100MB | 200MB | 钱包服务 |
| **系统预留** | 500MB | 500MB | 操作系统 + Docker守护进程 |
| **总计** | **1.15GB** | **1.7GB** | 剩余约300MB缓冲 |

### CPU使用估算（1核心）

- **空闲时**: 5-10%
- **正常负载**: 30-50%
- **峰值负载**: 70-90%
- **瓶颈**: 单核心，无法并行处理大量请求

---

## 🚦 限流配置分析

### Nginx限流（第一层防护）

| 端点类型 | 限流规则 | 说明 |
|---------|---------|------|
| 通用API | 10 req/s per IP | 基础API请求 |
| 聊天接口 | 5 req/s per IP | 主要业务接口 |
| 图片生成 | 12 req/min per IP | 昂贵操作 |
| 视频生成 | 12 req/min per IP | 非常昂贵 |
| 认证接口 | 2 req/s per IP | 防止暴力破解 |

### Node.js限流（第二层防护）

- **通用限流**: 100 req/min per IP
- **认证限流**: 20 req/min per IP
- **昂贵操作**: 10 req/min per IP

---

## 📈 承载能力评估

### 场景1: 纯聊天应用（无图片/视频生成）

**重要概念区分**:
- **同时在线用户**: 打开应用/网页的用户总数（可能只是浏览，不发送请求）
- **并发用户**: 同时发送API请求的用户（正在等待响应的用户）
- **日活用户**: 一天内使用过应用的用户总数

**假设条件**:
- 每个用户平均每天发送 20 条消息
- 每条消息平均响应时间 2-3 秒（LLM调用）
- 用户活跃时间集中在 8 小时（9:00-17:00）
- 同时在线率 = 日活用户数 × 20-30%（同时打开应用的用户比例）
- 并发率 = 同时在线用户 × 30-50%（同时发送请求的用户比例）

**计算**:
- 单核心可同时处理约 3-5 个聊天请求（每个2-3秒）
- 限流：5 req/s per IP，理论上可支持 5 个并发请求
- 但考虑到LLM是外部API调用，实际瓶颈在等待时间
- **关键**: 大部分用户只是浏览，不发送请求，所以同时在线用户可以更多

**结论**: 
- **日活用户**: **200-300人**
- **同时在线用户**: **40-90人**（20-30%的日活用户同时打开应用）
- **并发用户**: **12-45人**（30-50%的在线用户同时发送请求）
- **每小时请求数**: 约 500-750 次

### 场景2: 包含图片生成

**假设条件**:
- 10% 的用户会生成图片
- 图片生成平均耗时 10-15 秒
- 限流：12 req/min per IP

**影响**:
- 图片生成会占用较长时间，但限流严格
- 对整体承载影响：**-20%**

**结论**:
- **日活用户**: **160-240人**
- **同时在线用户**: **32-72人**
- **并发用户**: **10-36人**

### 场景3: 包含视频生成

**假设条件**:
- 5% 的用户会生成视频
- 视频生成平均耗时 30-60 秒
- 限流：12 req/min per IP

**影响**:
- 视频生成非常耗时，但用户比例低
- 对整体承载影响：**-10%**

**结论**:
- **日活用户**: **140-220人**
- **同时在线用户**: **28-66人**
- **并发用户**: **8-33人**

---

## ⚠️ 瓶颈分析

### 1. 内存瓶颈
- **风险**: ⚠️ **中等**
- **说明**: 2GB内存接近满载，MongoDB数据增长会导致内存压力
- **建议**: 
  - 定期清理日志
  - 优化MongoDB索引
  - 考虑使用MongoDB Atlas（云数据库）

### 2. CPU瓶颈
- **风险**: ⚠️⚠️ **高**
- **说明**: 单核心无法并行处理，大量请求会排队
- **表现**: 
  - 响应时间变长
  - 请求超时增加
  - 用户体验下降
- **建议**: 
  - 升级到2核或4核
  - 使用Node.js集群模式（需要多核）

### 3. 数据库连接池
- **当前配置**: `maxPoolSize: 10`
- **风险**: ⚠️ **中等**
- **说明**: 10个连接可能不足以支持高并发
- **建议**: 根据实际负载调整到 15-20

### 4. 外部API依赖
- **风险**: ⚠️ **低**
- **说明**: LLM、图片生成等依赖外部服务，不占用服务器资源
- **优势**: 主要计算在外部，服务器只做协调

---

## 📊 实际承载能力总结

### 保守估计（推荐）

| 指标 | 数值 | 说明 |
|------|------|------|
| **日活用户(DAU)** | **150-200人** | 包含所有功能使用 |
| **同时在线用户** | **30-60人** | 同时打开应用的用户（浏览+使用） |
| **并发用户** | **9-30人** | 同时发送API请求的用户 |
| **每小时请求** | **400-600次** | 平均 |
| **峰值请求** | **1000次/小时** | 短时间峰值 |

**重要说明**:
- ✅ **同时在线用户可以是30-60人**（只是打开应用，不一定都在发请求）
- ✅ **并发用户9-30人**（同时发送请求，等待响应）
- ✅ 大部分用户只是浏览，不发送请求，所以同时在线用户数可以更高

### 乐观估计（需监控）

| 指标 | 数值 | 说明 |
|------|------|------|
| **日活用户(DAU)** | **250-300人** | 如果用户行为分散 |
| **同时在线用户** | **50-90人** | 峰值时段同时打开应用 |
| **并发用户** | **15-45人** | 峰值时段同时发送请求 |
| **每小时请求** | **600-800次** | 平均 |
| **峰值请求** | **1500次/小时** | 短时间峰值 |

---

## 🎯 优化建议

### 短期优化（不升级硬件）

1. **启用MongoDB索引优化**
   ```javascript
   // 确保常用查询字段有索引
   db.messages.createIndex({ agentId: 1, userId: 1, createdAt: -1 })
   db.users.createIndex({ username: 1 })
   ```

2. **增加数据库连接池**
   ```javascript
   maxPoolSize: 15  // 从10增加到15
   ```

3. **启用Nginx缓存**
   ```nginx
   # 缓存静态资源和API响应
   proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=api_cache:10m;
   ```

4. **优化日志输出**
   - 减少不必要的console.log
   - 使用日志级别控制
   - 定期清理日志文件

5. **使用CDN**
   - 静态资源（图片、视频）使用CDN
   - 减少服务器带宽压力

### 中期优化（考虑升级）

1. **升级到2核4G**
   - 日活可提升到 **500-800人**
   - 并发用户提升到 **50-80人**

2. **分离数据库**
   - 使用MongoDB Atlas（云数据库）
   - 释放服务器内存约300-500MB
   - 提升数据库性能

3. **使用Redis缓存**
   - 缓存热点数据
   - 减少数据库查询

### 长期优化（架构升级）

1. **微服务拆分**
   - 聊天服务独立
   - 图片/视频生成服务独立
   - 钱包服务独立

2. **负载均衡**
   - 多实例部署
   - 自动扩缩容

---

## 📈 监控指标建议

### 关键指标

1. **内存使用率**
   - 阈值: < 85%
   - 告警: > 90%

2. **CPU使用率**
   - 阈值: < 70%
   - 告警: > 85%

3. **响应时间**
   - 平均: < 2秒
   - P95: < 5秒
   - 告警: > 10秒

4. **错误率**
   - 阈值: < 1%
   - 告警: > 5%

5. **数据库连接数**
   - 阈值: < 8/10
   - 告警: > 9/10

### 监控工具

- **Docker Stats**: `docker stats --no-stream`
- **健康检查**: `/api/health` 端点
- **日志监控**: Docker logs + 日志聚合

---

## 🎯 最终结论

### 当前配置（1核2G）承载能力

**推荐日活用户数**: **150-200人**

**同时在线用户数**: **30-60人**（可以更多，因为大部分只是浏览）

**并发用户数**: **9-30人**（同时发送请求的用户）

**理由**:
1. ✅ 内存使用率控制在85%以内
2. ✅ CPU有足够缓冲应对峰值
3. ✅ 限流机制保护服务器
4. ✅ 外部API依赖减轻服务器压力
5. ✅ **关键**: 大部分用户只是浏览，不发送请求，所以同时在线用户数可以更高

**重要澄清**:
- ❌ **不是**"最多1-2个用户同时在线"
- ✅ **实际是**"30-60个用户可以同时在线"（打开应用）
- ✅ **其中**"9-30个用户同时发送请求"（并发）
- ✅ 大部分用户只是浏览界面、查看历史消息，不占用服务器资源

**风险提示**:
- ⚠️ 超过200日活需要密切监控
- ⚠️ 超过250日活建议升级硬件
- ⚠️ 峰值时段可能出现响应延迟

### 升级建议

| 日活用户数 | 推荐配置 | 成本估算 |
|-----------|---------|---------|
| 150-200 | 1核2G（当前） | 基础 |
| 300-500 | 2核4G | +50% |
| 500-1000 | 4核8G | +150% |
| 1000+ | 8核16G + 负载均衡 | +300% |

---

## 📝 测试建议

1. **压力测试**
   ```bash
   # 使用Apache Bench或wrk进行压力测试
   ab -n 1000 -c 10 https://cling-ai.com/api/health
   ```

2. **监控实际使用**
   - 部署后观察1-2周
   - 记录峰值时段资源使用
   - 根据实际数据调整

3. **设置告警**
   - CPU > 85%
   - 内存 > 90%
   - 响应时间 > 10秒
   - 错误率 > 5%

---

**报告生成时间**: 2025-12-15
**评估基准**: 当前代码和配置
