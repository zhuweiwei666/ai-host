# 监听端口
PORT=4100

# MongoDB 连接串（AI Wallet 单独的库，可以和主系统共享同一个 Cluster，不一定共享同一个 DB）
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/<db-name>?retryWrites=true&w=majority&appName=ai-wallet

# （可选）调用方服务名，仅用来日志标记
SERVICE_NAME=ai-wallet

# （可选）简单级别的服务调用 API Key（如果未来只允许你的主后端来调用钱包）
# 前期如果麻烦，可以先留空，逻辑里检测为空就跳过校验
WALLET_SERVICE_KEY=changeme-wallet-service-key
